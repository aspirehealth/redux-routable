{
  "name": "redux-routable",
  "version": "1.0.4",
  "description": "Simple Routing Middleware for Redux",
  "main": "lib/index.js",
  "author": "Jamin Thornsberry",
  "license": "MIT",
  "scripts": {
    "build": "rimraf src && babel lib --root-mode upward --out-dir src --ignore '**/*.spec.js' && pika build"
  },
  "@pika/pack": {
    "pipeline": [
      ["@pika/plugin-standard-pkg"],
      ["@pika/plugin-build-web"]
    ]
  },
  "dependencies": {
    "@babel/runtime": "^7.8.3",
    "path-to-regexp": "^6.1.0",
    "query-string": "^6.10.1"
  },
  "peerDependencies": {
    "history": "^4.9.0"
  }
}
