{
  "name": "redux-routable",
  "version": "1.0.4",
  "description": "Simple Routing Middleware for Redux",
  "author": "Jamin Thornsberry",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "build": "rimraf src && babel lib --out-dir src --ignore '**/*.spec.js' && pika build"
  },
  "@pika/pack": {
    "pipeline": [
      ["@pika/plugin-standard-pkg"],
      ["@pika/plugin-build-node"],
      ["@pika/plugin-build-web"]
    ]
  },
  "dependencies": {
    "@babel/runtime": "^7.9.2",
    "path-to-regexp": "^6.1.0",
    "query-string": "^6.11.1"
  },
  "peerDependencies": {
    "history": "^4.9.0"
  }
}
